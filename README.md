# waste-management

A simple waste management system available as a website (Java Spring Boot).

## Prerequisites

You need to have **Maven** installed. On macOS, you can install it using Homebrew:
```bash
brew install maven
```

## Usage

Run the application:
```bash
mvn spring-boot:run
```